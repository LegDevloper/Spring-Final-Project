<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="site.metacoding.SpringFinalProject.domain.kickout.KickoutDao">
  <insert id="insert"></insert>
  <select id="findById" resultType="site.metacoding.SpringFinalProject.domain.kickout.Kickout">
  	SELECT * FROM player WHERE id = #{id}	
  </select>
  <select id="findAll" resultType="site.metacoding.SpringFinalProject.domain.kickout.Kickout">
  	SELECT * FROM player
  </select>
  <update id="update"></update>
  <delete id="deleteById"></delete>
  </mapper>