<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.SpringFinalProject.domain.player.PlayerDao">
	<insert id="insert">
		INSERT INTO player(teamName,playerName,position,createdAt)
		VALUES(#{teamName},#{playerName},#{position}, NOW())
	</insert>
	<select id="findById"
		resultType="site.metacoding.SpringFinalProject.domain.player.Player">
		SELECT * FROM player WHERE id = #{id}
	</select>
	<select id="findAll"
		resultType="site.metacoding.SpringFinalProject.web.dto.response.player.MainDto">
		SELECT ROW_NUMBER() OVER() AS ROWNO, teamName,playerName,POSITION, TO_CHAR(createdAt, 'YYYY-MM-DD') as createdAT
		FROM player
	</select>

	<update id="update"></update>
	<delete id="deleteById"></delete>
</mapper>