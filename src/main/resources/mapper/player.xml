<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.SpringFinalProject.domain.player.PlayerDao">
	<insert id="insert">
		INSERT INTO player(teamName,playerName,posi,is_out,created_at)
		VALUES(#{teamName},#{playerName},#{posi},0, NOW())
	</insert>
	<select id="findById" resultType="site.metacoding.SpringFinalProject.domain.player.Player">
		SELECT * FROM player WHERE id = #{id}
	</select>
	<select id="findAll"
		resultType="site.metacoding.SpringFinalProject.web.dto.response.player.MainDto">
		SELECT ROW_NUMBER() OVER() AS ROWNO, id, teamName,playerName,posi,
		TO_CHAR(created_at,
		'YYYY-MM-DD') as createdAT
		FROM player
	</select>
	<select id="findToId" resultType="Integer">
		SELECT id FROM (SELECT ROW_NUMBER() OVER() AS ROWNO,id,
		teamName,playerName,posi, TO_CHAR(created_at, 'YYYY-MM-DD') as createdAT
		FROM player) player
		WHERE
		rowno=#{rowno}
	</select>

	<select id="findToName" resultType="String">
		SELECT playerName FROM player WHERE id=#{id}
	</select>
	<update id="update">
		UPDATE player SET teamName=#{teamName}, playerName=#{playerName}, posi=#{posi}
		WHERE id=#{id}
	</update>
	<delete id="deleteById">
		DELETE FROM player WHERE id=#{id}
	</delete>
</mapper>