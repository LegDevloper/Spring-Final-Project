<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.SpringFinalProject.domain.team.TeamDao">
	<insert id="insert">
		INSERT INTO team(teamName,stadiumName,createdAt)
		VALUES(#{teamName},#{stadiumName},NOW())
	</insert>
	<select id="findToId"
		resultType="Integer">
		SELECT id FROM (SELECT ROW_NUMBER() OVER() AS ROWNO,id, teamName, stadiumName,
		TO_CHAR(createdAt, 'YYYY-MM-DD') as createdAT
		FROM team) team WHERE ROWNO=#{rowno}
	</select>
	<select id="findById"
		resultType="String">
		SELECT teamName FROM team WHERE id = #{id}
	</select>
	<select id="findAll"
		resultType="site.metacoding.SpringFinalProject.web.dto.response.team.MainDto">
		SELECT ROW_NUMBER() OVER() AS ROWNO, id, teamName, stadiumName,
		TO_CHAR(createdAt, 'YYYY-MM-DD') as createdAT
		FROM team
	</select>
	<select id="findToTeamName"
		resultType="site.metacoding.SpringFinalProject.web.dto.response.team.TeamListDto">
		SELECT teamName
		FROM team
	</select>
	<update id="update">
		UPDATE team SET teamName=#{teamName}, stadiumName=#{stadiumName} WHERE id=#{id}
	</update>
	<delete id="deleteById">
		DELETE FROM team WHERE id=#{id}	
	</delete>
</mapper>